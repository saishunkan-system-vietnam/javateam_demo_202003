<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vn.de.example.parkingregistration.model.mapper.AppRoleModelMapper">

        <select id="getRoleNames" resultType="String">
            select role_name from app_role where role_id = #{id}
        </select>

</mapper>
