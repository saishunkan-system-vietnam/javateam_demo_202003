<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="vn.de.example.parkingregistration.model.mapper.StudentModelMapper">

	<select id="findAllStudent" resultType="Student">
		select * from student
	</select>
	
	<select id="findByIdStudent" parameterType="int" resultType="Student">
		select * from student where id =#{id};
	</select>
	<insert id="addStudent" parameterType="Student">
	Insert into student (id,name,branch,phone) values (#{id},#{name},#{branch},#{phone});
	</insert> 
	<delete id="deleteStudent" parameterType="int">
		DELETE from student
		WHERE id = #{id};
	</delete>
	<update id="updateStudent" parameterType="Student">
		UPDATE student SET
		name = #{name}, branch = #{branch}, phone = #{phone} WHERE id = #{id};
	</update>
	
	<select id="search" resultType="Student">
		select * from student where 1=1
		<if test="name != null">
		   AND name like '%${name}%'
		</if>

		<if test="branch != null">
			AND branch like '%${branch}%'
		</if>
	</select>
	<select id="findStudent" resultType="Student">

	</select>

	<resultMap type="Student" id="student">
		<id column="id" property="id" />
		<result column="name" property="name" />
		<result column="branch" property="branch" />
		<result column="phone" property="phone" />
		<!-- <collection property="subjects" ofType="Subject">
        <result property="idSub" column="id_sub"/>
        <result column="sub" property="sub" />
    </collection>
    <collection property="studentSubjects" ofType="StudentSubject">
    <result column="point_sub" property="pointSub" />
    </collection> -->
	</resultMap>
</mapper>
